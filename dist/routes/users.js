"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = require("express");
const userController_1 = require("../controllers/userController");
const auth_1 = require("../middleware/auth");
const multer_1 = __importDefault(require("multer"));
const path_1 = __importDefault(require("path"));
const storage = multer_1.default.diskStorage({ destination: 'uploads/avatars', filename: (_, file, cb) => cb(null, Date.now() + path_1.default.extname(file.originalname)) });
const upload = (0, multer_1.default)({ storage, limits: { fileSize: 2 * 1024 * 1024 } });
const router = (0, express_1.Router)();
router.use(auth_1.protect);
router.get('/', auth_1.adminOnly, userController_1.getAllUsers);
router.get('/departments', userController_1.getDepartments);
router.get('/:id', userController_1.getUserById);
router.post('/', auth_1.adminOnly, userController_1.createUser);
router.put('/profile', upload.single('avatar'), userController_1.updateProfile);
router.put('/:id', auth_1.adminOnly, userController_1.updateUser);
router.delete('/:id', auth_1.adminOnly, userController_1.deleteUser);
exports.default = router;
